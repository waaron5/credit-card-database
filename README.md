An open-source dataset of credit cards used by the REWRDS credit card recommendation platform.
Each card entry follows the schema in `schema/card.schema.json`.
Anyone can contribute cards or updates via pull request.
